{
  "schema_version": "kinofail.realistic-multimodal-protocol.v1",
  "protocol_id": "kinofail_realistic_multimodal_scale_v7_v1",
  "status": "frozen_before_complete_scale_v7_outcomes",
  "freeze_context": "Architecture and splits fixed after runtime QA of the first 17 scale attempts; no formal snapshot bundle, CLIP feature cache, model training, or heldout model evaluation existed.",
  "source_snapshot_protocol": "configs/eval/kinofail_realistic_snapshot_formal_v1.json",
  "source_feature_manifest": "outputs/eval/realistic_a0_a7_v4/features/feature_manifest.json",
  "scene_registry": "configs/data/kinofail_realistic_scale_scene_registry_v1.json",
  "output_root": "outputs/eval/realistic_a0_a7_v4",
  "training_seeds": [0, 1, 2, 3, 4],
  "methods": [
    "vision_only",
    "proprio_only",
    "early_fusion",
    "structured_bidirectional"
  ],
  "fixed_model": {
    "base_estimator": "ExtraTreesClassifier",
    "n_estimators": 192,
    "max_features": "sqrt",
    "min_samples_leaf": 2,
    "class_weight": "balanced",
    "structured_gate_threshold": 0.5,
    "structured_meta": "three-fold StratifiedGroupKFold expert probabilities -> class-balanced logistic regression"
  },
  "heldout_axes": {
    "scene": {
      "fit": "registry split in {train,val}; all appearance views; physical-episode weights sum to one",
      "test": "registry split test; primary views for headline; all views only for texture consistency"
    },
    "material": {
      "fit": "primary material prefix in {train,val}; primary views only",
      "test": "primary material prefix test; primary views only"
    },
    "scene_and_material": {
      "fit": "registry and primary material splits both in {train,val}; primary views only",
      "test": "registry and primary material splits both test; primary views only"
    },
    "camera_profile": {
      "fit": "Go2-front calibration profiles A/B; primary views only",
      "test": "Go2-front calibration profile C; primary views only"
    }
  },
  "leakage_guards": [
    "no appearance view from a test physical episode enters the scene-heldout fit",
    "material-heldout axes use primary views only so identical proprioception cannot cross the split",
    "scene/operator/severity/appearance IDs and privileged telemetry are forbidden model inputs",
    "appearance swaps are not independent physical samples"
  ],
  "a3_cells": {
    "T1": ["O1_mu_field", "O3_collapse", "O9_high_centering"],
    "T2": ["O2_compliance", "O4_tether"],
    "T3": ["O7_visual_remap", "O8_invisible_collider"],
    "T4": ["O5_payload", "O6_push", "O10_effort_decay", "O11_obs_bias"],
    "T5": "all nominal_counterfactual episodes"
  },
  "a3_missing_design_guard": {
    "reverse_conflict": "must be reported missing until a separately frozen O7 hazard-looking/nominal realization is collected",
    "may_not_relabel_ordinary_O7_nominal_counterfactual_as_reverse": true
  },
  "bootstrap": {"repetitions": 10000, "seed": 20260723},
  "a8_in_scope": false
}
