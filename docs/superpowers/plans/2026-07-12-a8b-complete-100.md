# A8b Complete-to-100% Implementation Plan

> **For agentic workers:** Execute task-by-task. Steps use checkbox syntax.

**Goal:** Bring A8b Cross-Modal RoboFail/REFLECT transfer to design-complete 100% against `A8_design.md`, regenerate real artifacts, then QA.

**Architecture:** Rebuild conflict-certified multi-window REFLECT cards (E1–E4 + depth + recovery labels), unify A8b eval with rule/learned/trained arms + task-family LOO CLIP+state, emit calibration/recovery metrics, train missing same-backbone adapters, regenerate summary/report, and certify with tests + QA checklist.

**Tech Stack:** Python 3.11, torch/CLIP, Qwen3-VL-4B LoRA SFT, REFLECT real zarr, pytest.

## Global Constraints
- Real REFLECT multisensory only for headlines (no surrogate substitution).
- Leakage firewall: model inputs exclude labels/reasons/rewards/captions/stratum.
- Same-backbone LoRA budget fixed from `configs/eval/a8.yaml`.
- Honest nulls allowed; do not fabricate Δfusion/Δconflict gains.
- Config hash + commit + source hashes on every artifact.

---

### Task 1: Expand A8b cards to E1 + depth + recovery + task family

**Files:**
- Modify: `kino_vla/eval/a8_reflect.py`
- Modify: `kino_vla/eval/a8_strata.py` (if needed)
- Modify: `tests/test_a8_reflect.py`
- Modify: `scripts/a8_build_datasets.py` (if needed)

**Produces:** cards with strata {E1,E2,E3,E4}, optional depth paths, `task_family`, `recommended_recovery_class` (eval-only), provenance fields.

- [ ] E1 agree-failure: failure_end window when both channels informative (high force/motion + failure reason present).
- [ ] Keep E2 state_nominal_mid / E3 state_hot_end / E4 early_nominal.
- [ ] Load depth if present; store paths + depth summary stats (allowed as observable).
- [ ] Pre-register recovery compiler from failure_reason/mode → benchmark-native class.
- [ ] Unit tests for E1 emission and leakage (recovery fields not in model_input).

### Task 2: A8b eval completeness (arms, LOO, calibration, recovery, oracle)

**Files:**
- Modify: `scripts/a8_eval_a8b_real.py`
- Modify: `kino_vla/eval/a8_metrics.py`
- Create/Modify tests under `tests/test_a8_*.py`

**Produces:** complete `summary.json` with all design metrics.

- [ ] Arms: v_only, p_only, concat, text, latent, latent_conflict, rule_*, learned_conflict, learned_clip_state (task-family LOO), trained_*, oracle_modality.
- [ ] CLIP+state features from real RGB + state; confidence = max(p,1-p); provenance manifest.
- [ ] LOO grouped by task family (not only episode).
- [ ] Calibration: ECE / confidence-correct AUROC / risk-coverage / AURC when conf present.
- [ ] Recovery admissibility: if pred=failure, recovery class admissible for truth cause; report gated accuracy.
- [ ] Fix rule_pareto_gap consistency with arms.* rates.
- [ ] Bootstrap + McNemar vs V and best unimodal; CBA deltas.

### Task 3: Train + infer missing same-backbone A8b arms

**Files:**
- Use: `scripts/a8_train.py`, `scripts/a8_infer.py`
- Adapters under `outputs/eval/a8/adapters/a8b_{concat,latent,latent_conflict}/seed0`

- [ ] Rebuild dataset after Task 1.
- [ ] Train concat / latent / latent_conflict (seed0) if missing.
- [ ] Infer all trained arms onto A8b cards.
- [ ] Re-run a8_eval_a8b_real + a8_report.

### Task 4: Report, completeness gate, QA

**Files:**
- Modify: `scripts/a8_report.py`, `A实验/A8.md`, `CLAUDE.md`
- Write: `outputs/eval/a8/a8b/QA_A8B.md`

- [ ] Report E1–E4 table, recovery, calibration, provenance, honest nulls.
- [ ] Completeness checklist ALL_PASS or explicit residual.
- [ ] `pytest -k a8` green; ruff on touched files.
- [ ] Update CLAUDE §2/§4.

## Definition of 100% (A8b design-complete)
1. Real multisensory admission pass
2. E1+E2+E3+E4 all present with n>0
3. RGB (+depth if available) + proprio inputs
4. Full arm roster evaluated with artifacts
5. CBA + Δfusion + Δconflict reported (even if 0)
6. Calibration diagnostics present
7. Benchmark-native recovery admissibility present
8. Task-family LOO learned fusion with provenance
9. Leakage tests pass
10. Report + QA + CLAUDE updated
